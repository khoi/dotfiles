[extend]
useDefault = true

[[allowlists]]
description = "ghostty keybind definitions trip the generic-api-key rule"
targetRules = ["generic-api-key"]
regexTarget = "match"
regexes = ['''^keybind = ''']
