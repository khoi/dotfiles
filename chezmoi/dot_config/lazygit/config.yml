customCommands:
  - key: "<c-s>"
    context: "global"
    command: "GIT_EDITOR=: git commit --no-verify"
    loadingText: "Generating commit message"
  - key: "<c-c>"
    context: "global"
    command: "GIT_EDITOR=: git commit --no-verify"
    loadingText: "Generating commit message"
  - key: "<c-q>"
    context: "global"
    command: 'rm -f "$(git rev-parse --git-dir)/index.lock"'
    loadingText: "Removing git index lock"
  - key: "<c-u>"
    context: "global"
    command: "git submodule update --force"
    loadingText: "Updating git submodules with force"

git:
  autoFetch: false
  autoRefresh: false

gui:
  screenMode: half
  spinner:
    frames:
      - "⢿"
      - "⣻"
      - "⣽"
      - "⣾"
      - "⣷"
      - "⣯"
      - "⣟"
      - "⡿"
